<div>
  <iframe class="webgl_example" style="width: {{width}}px; height: {{height}}px;" src="{{url}}"></iframe>
  <a class="webgl_center" href="{{url}}" target="_blank">click here to open in a separate window</a>
</div>

